node_modules/
dist/
outputs/
work/

.DS_Store
.env
.env.*
!.env.example
*.log

coverage/
.vite/

.traselveloreal-sync-state.json

# Firebase service-account keys must never be committed -- backend only,
# stored as a Render Secret File.
*firebase-adminsdk*.json

# Local agent-handover notes -- session-to-session scratch, not project docs.
AGENTS.md
FOR_CODEX.md
