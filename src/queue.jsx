import React, { createContext, useCallback, useContext, useEffect, useMemo, useState } from 'react';
import ReactDOM from 'react-dom/client';
import { ArrowLeft, BarChart3, CalendarDays, ChevronLeft, ChevronRight, LoaderCircle, LogOut, Moon, Palette, Pencil, Send, Settings, Sun, X } from 'lucide-react';
import { browserPopupRedirectResolver, getRedirectResult, onAuthStateChanged, signOut } from 'firebase/auth';
import { describeSignInError, firebaseAuth as auth, startGoogleSignIn } from './firebase';
import { clearSsoCookie, startSsoRefresh, trySsoSignIn } from './sso';
import { API_BASE, apiFetch } from './api';
import './queue.css';

const START = 480;
const DAY = (value = new Date()) => `${value.getFullYear()}-${String(value.getMonth() + 1).padStart(2, '0')}-${String(value.getDate()).padStart(2, '0')}`;
const shiftDay = (date, amount) => { const value = new Date(`${date}T12:00:00`); value.setDate(value.getDate() + amount); return DAY(value); };
const time = (minutes) => { const normalized = ((minutes % 1440) + 1440) % 1440; return `${String(Math.floor(normalized / 60)).padStart(2, '0')}:${String(normalized % 60).padStart(2, '0')}`; };
const currentMinutes = (value = new Date()) => value.getHours() * 60 + value.getMinutes();
const displayDate = (value) => new Date(`${value}T12:00:00`).toLocaleDateString(undefined, { weekday: 'long', month: 'short', day: 'numeric' });
const cover = (task) => task?.post?.coverUrl ? `${API_BASE}${task.post.coverUrl}` : '';

const COPY = {
  en: { productionQueue: 'Production Queue', dashboard: 'Dashboard', admin: 'Admin', coordinatorSchedule: 'Coordinator schedule', mySchedule: 'My production schedule', today: 'Today', submit: 'Submit', change: 'change', changes: 'changes', productionPool: 'Production pool', readyToSchedule: 'ready to schedule', visibleSchedule: 'Drag requests into the visible 12-hour schedule.', emptyPool: 'No requests are waiting in the pool.', myAssignedWork: 'My assigned work', upcomingProduction: 'Upcoming production', activeRequest: 'active request', activeRequests: 'active requests', noActiveAssignments: 'No active assignments', emptyAssignments: 'When a coordinator schedules work for you, it will appear here.', post: 'Post', scheduled: 'Scheduled', deadline: 'Deadline', scope: 'Scope', status: 'Status', noTags: 'No tags', designer: 'Designer', now: 'Now', loadingSchedule: 'Loading schedule…', tryAgain: 'Try again', queueAccess: 'Queue is available to production coordinators and designers.', rolePreview: 'Role preview', onlyEsteban: 'Only visible to Esteban.', activeRole: 'Active role', devFullAccess: 'Dev · full access', postDesigner: 'Post Designer', viralCoordinator: 'Viral Coordinator', productionReports: 'Production reports', adminWorkspace: 'Admin workspace', loadingReport: 'Loading report…', inPool: 'In pool', inProgress: 'In progress', readyToClose: 'Ready to close', closed: 'Closed', designerWorkload: 'Designer workload', workloadHelp: 'Active requests and planned production points.', openSettings: 'Open dashboard settings', startWork: 'Start work', markComplete: 'Mark complete', publishedLink: 'Published Instagram link', closeRequest: 'Close request', returnInProgress: 'Return to in progress', openPublished: 'Open published post', cancellationReason: 'Cancellation reason (optional)', cancelRequest: 'Cancel request', brief: 'Brief', notes: 'Notes', references: 'References', minutes: 'minutes', sourcePost: 'Source post', assignment: 'Assignment', recommendedAccount: 'Recommended account', editRequest: 'Edit request', saveChanges: 'Save changes', cancel: 'Cancel', productionPoints: 'Production points', tags: 'Tags', referenceLinks: 'Reference links', oneLinkPerLine: 'One link per line', signIn: 'Sign in with Google', signingIn: 'Signing in…', signInHelp: 'Sign in with Google to open your production schedule.' },
  es: { productionQueue: 'Cola de producción', dashboard: 'Dashboard', admin: 'Admin', coordinatorSchedule: 'Agenda de coordinación', mySchedule: 'Mi agenda de producción', today: 'Hoy', submit: 'Enviar', change: 'cambio', changes: 'cambios', productionPool: 'Pool de producción', readyToSchedule: 'listos para programar', visibleSchedule: 'Arrastra los requests al horario visible de 12 horas.', emptyPool: 'No hay requests esperando en el pool.', myAssignedWork: 'Mi trabajo asignado', upcomingProduction: 'Próxima producción', activeRequest: 'request activo', activeRequests: 'requests activos', noActiveAssignments: 'No tienes asignaciones activas', emptyAssignments: 'Cuando un coordinador programe trabajo para ti, aparecerá aquí.', post: 'Post', scheduled: 'Programado', deadline: 'Deadline', scope: 'Alcance', status: 'Estado', noTags: 'Sin tags', designer: 'Designer', now: 'Ahora', loadingSchedule: 'Cargando agenda…', tryAgain: 'Intentar de nuevo', queueAccess: 'Queue está disponible para coordinadores y diseñadores de producción.', rolePreview: 'Vista de rol', onlyEsteban: 'Visible solo para Esteban.', activeRole: 'Rol activo', devFullAccess: 'Dev · acceso completo', postDesigner: 'Post Designer', viralCoordinator: 'Viral Coordinator', productionReports: 'Reportes de producción', adminWorkspace: 'Espacio Admin', loadingReport: 'Cargando reporte…', inPool: 'En pool', inProgress: 'En progreso', readyToClose: 'Listo para cerrar', closed: 'Cerrado', designerWorkload: 'Carga por designer', workloadHelp: 'Requests activos y puntos de producción planeados.', openSettings: 'Abrir Settings del dashboard', startWork: 'Empezar trabajo', markComplete: 'Marcar como completado', publishedLink: 'Link publicado de Instagram', closeRequest: 'Cerrar request', returnInProgress: 'Volver a en progreso', openPublished: 'Abrir post publicado', cancellationReason: 'Motivo de cancelación (opcional)', cancelRequest: 'Cancelar request', brief: 'Brief', notes: 'Notas', references: 'Referencias', minutes: 'minutos', sourcePost: 'Post original', assignment: 'Asignación', recommendedAccount: 'Cuenta recomendada', editRequest: 'Editar request', saveChanges: 'Guardar cambios', cancel: 'Cancelar', productionPoints: 'Puntos de producción', tags: 'Tags', referenceLinks: 'Links de referencia', oneLinkPerLine: 'Un link por línea', signIn: 'Iniciar sesión con Google', signingIn: 'Iniciando sesión…', signInHelp: 'Inicia sesión con Google para abrir tu agenda de producción.' },
};
const QueuePreferencesContext = createContext({ language: 'en', t: (key) => key });
const useQueuePreferences = () => useContext(QueuePreferencesContext);
const statusCopy = (status, t) => ({ pool: t('inPool'), scheduled: t('scheduled'), in_progress: t('inProgress'), completed: t('readyToClose'), closed: t('closed') }[status] || status);

async function json(path, options) {
  const response = await apiFetch(`${API_BASE}${path}`, options);
  const body = await response.json().catch(() => ({}));
  if (!response.ok) throw new Error(body.detail || 'Queue could not complete that action.');
  return body;
}

function AuthGate({ notice, setNotice }) {
  const { t } = useQueuePreferences();
  const [busy, setBusy] = useState(false);
  const signIn = async () => { setBusy(true); const error = await startGoogleSignIn(); if (error) setNotice(describeSignInError(error)); setBusy(false); };
  return <main className="queue-auth"><section><h1>{t('productionQueue')}</h1><p>{notice || t('signInHelp')}</p><button type="button" onClick={signIn} disabled={busy}>{busy ? t('signingIn') : t('signIn')}</button></section></main>;
}

function QueuePreferences() {
  const { language, setLanguage, theme, setTheme, accent, setAccent } = useQueuePreferences();
  return <div className="queue-preferences"><div className="queue-language" aria-label="Language"><button type="button" className={language === 'en' ? 'is-on' : ''} onClick={() => setLanguage('en')}>EN</button><button type="button" className={language === 'es' ? 'is-on' : ''} onClick={() => setLanguage('es')}>ES</button></div><button type="button" className="queue-theme-toggle" onClick={() => setTheme(theme === 'dark' ? 'light' : 'dark')} title={theme === 'dark' ? 'Use light theme' : 'Use dark theme'}>{theme === 'dark' ? <Sun size={14} /> : <Moon size={14} />}</button><div className="queue-accent-picker" title="Accent color"><Palette size={13} />{['lime', 'blue', 'coral'].map((value) => <button type="button" key={value} className={`accent-${value}${accent === value ? ' is-on' : ''}`} onClick={() => setAccent(value)} aria-label={`${value} accent`} />)}</div></div>;
}

function DevRolePreview({ isDev }) {
  const { t } = useQueuePreferences();
  const [open, setOpen] = useState(false);
  const active = window.localStorage.getItem('sentient.queueRolePreview') || '';
  if (!isDev) return null;
  const label = { sales: 'Sales', pd: t('postDesigner'), vc: t('viralCoordinator'), admin: t('admin') }[active] || 'Dev';
  const choose = (event) => {
    const role = event.target.value;
    if (role) window.localStorage.setItem('sentient.queueRolePreview', role);
    else window.localStorage.removeItem('sentient.queueRolePreview');
    window.location.reload();
  };
  return <div className="dev-role-preview"><button type="button" onClick={() => setOpen((value) => !value)} aria-expanded={open}><span>DEV</span>{label}</button>{open ? <div className="dev-role-preview-panel"><strong>{t('rolePreview')}</strong><p>{t('onlyEsteban')}</p><label>{t('activeRole')}<select value={active} onChange={choose}><option value="">{t('devFullAccess')}</option><option value="sales">Sales</option><option value="pd">{t('postDesigner')}</option><option value="vc">{t('viralCoordinator')}</option><option value="admin">{t('admin')}</option></select></label></div> : null}</div>;
}

function TaskBlock({ task, editable, onOpen, onDragStart, viewStart }) {
  const left = Math.max(0, (task.scheduledStartMinutes ?? START) - viewStart);
  const planned = task.durationMinutes || 10;
  let width = planned;
  if (['completed', 'closed'].includes(task.status) && task.actualStartedAt && task.completedAt) width = Math.min(planned, Math.max(10, Math.round((new Date(task.completedAt) - new Date(task.actualStartedAt)) / 60000)));
  return <button type="button" draggable={editable && task.status !== 'in_progress'} className={`scheduler-block state-${task.status}${left + width > 720 ? ' is-extra' : ''}`} style={{ left: `${(left / 720) * 100}%`, width: `${(width / 720) * 100}%` }} onDragStart={(event) => { event.dataTransfer.setData('queue-task', String(task.id)); onDragStart(task.id); }} onClick={() => onOpen(task)} title={`${task.post.account} · ${task.productionPoints} PP · ${task.status}`}>
    {cover(task) ? <img src={cover(task)} alt="" /> : null}<span className="scheduler-block-copy"><b>@{task.post.account}</b><small>{task.productionPoints} PP · {time(task.scheduledStartMinutes || START)}</small></span>{task.recommendedAccounts?.length ? <span className="scheduler-account-bubbles">{task.recommendedAccounts.map((account) => <i key={account}>@{account.slice(0, 1)}</i>)}</span> : null}
  </button>;
}

function PoolCard({ task, onOpen, onDragStart }) {
  return <article className="queue-pool-card" draggable onDragStart={(event) => { event.dataTransfer.setData('queue-task', String(task.id)); onDragStart(task.id); }}><button type="button" onClick={() => onOpen(task)}>{cover(task) ? <img src={cover(task)} alt="" /> : <span className="queue-pool-empty">@</span>}<span><b>@{task.post.account}</b><small>{task.productionPoints} PP · {task.durationMinutes} min</small><em>{new Date(task.deadlineAt).toLocaleString()}</em></span></button><div>{task.tags?.map((tag) => <i key={tag}>{tag}</i>)}</div></article>;
}

function DesignerAssignments({ tasks, onOpen }) {
  const { t, language } = useQueuePreferences();
  return <section className="designer-assignments"><header><div><p className="scheduler-eyebrow">{t('myAssignedWork')}</p><h2>{t('upcomingProduction')}</h2></div><small>{tasks.length} {tasks.length === 1 ? t('activeRequest') : t('activeRequests')}</small></header>{tasks.length ? <div className="designer-assignment-table" role="table"><div className="designer-assignment-head" role="row"><span>{t('post')}</span><span>{t('scheduled')}</span><span>{t('deadline')}</span><span>{t('scope')}</span><span>{t('status')}</span></div>{tasks.map((task) => <button type="button" role="row" key={task.id} className={`designer-assignment-row state-${task.status}`} onClick={() => onOpen(task)}><span className="designer-assignment-post">{cover(task) ? <img src={cover(task)} alt="" /> : <span className="designer-assignment-empty">@</span>}<span><b>@{task.post.account}</b><small>{task.brief || task.post.caption || t('post')}</small></span></span><span className="designer-assignment-time"><b>{displayDate(task.scheduledDate)}</b><small>{time(task.scheduledStartMinutes ?? START)} · {task.durationMinutes} {t('minutes')}</small></span><span className="designer-assignment-deadline"><b>{new Date(task.deadlineAt).toLocaleDateString(language === 'es' ? 'es-CR' : undefined, { month: 'short', day: 'numeric' })}</b><small>{new Date(task.deadlineAt).toLocaleTimeString(language === 'es' ? 'es-CR' : undefined, { hour: '2-digit', minute: '2-digit' })}</small></span><span className="designer-assignment-pp"><b>{task.productionPoints} PP</b><small>{task.tags?.slice(0, 2).join(' · ') || t('noTags')}</small></span><span className="designer-assignment-status"><i>{statusCopy(task.status, t)}</i></span></button>)}</div> : <div className="designer-assignments-empty"><CalendarDays size={18} /><strong>{t('noActiveAssignments')}</strong><span>{t('emptyAssignments')}</span></div>}</section>;
}

function AdminTools({ report, loading, error, onClose }) {
  const { t } = useQueuePreferences();
  const totals = report?.totals || {};
  const metric = (status, label) => <div key={status}><span>{label}</span><strong>{totals[status]?.count || 0}</strong><small>{totals[status]?.points || 0} PP</small></div>;
  return <section className="queue-admin-tools"><header><div><p className="scheduler-eyebrow">{t('adminWorkspace')}</p><h2>{t('productionReports')}</h2></div><button type="button" onClick={onClose} aria-label="Close admin tools"><X size={16} /></button></header>{loading ? <div className="queue-admin-loading"><LoaderCircle className="queue-spin" />{t('loadingReport')}</div> : null}{error ? <p className="queue-admin-error">{error}</p> : null}{report ? <><div className="queue-admin-metrics">{metric('pool', t('inPool'))}{metric('scheduled', t('scheduled'))}{metric('in_progress', t('inProgress'))}{metric('completed', t('readyToClose'))}{metric('closed', t('closed'))}</div><div className="queue-admin-designers"><div><h3>{t('designerWorkload')}</h3><p>{t('workloadHelp')}</p></div><div className="queue-admin-designer-list">{report.designers.map((designer) => <span key={designer.email}><b>{designer.email.split('@')[0]}</b><small>{designer.activeRequests} {t('activeRequests')} · {designer.productionPoints} PP</small></span>)}</div></div><a className="queue-admin-settings" href={`${import.meta.env.BASE_URL}?view=admin`} target="_blank" rel="noreferrer"><Settings size={14} />{t('openSettings')}</a></> : null}</section>;
}

function localInputDate(value) {
  const date = new Date(value);
  if (Number.isNaN(date.getTime())) return '';
  const pad = (number) => String(number).padStart(2, '0');
  return `${date.getFullYear()}-${pad(date.getMonth() + 1)}-${pad(date.getDate())}T${pad(date.getHours())}:${pad(date.getMinutes())}`;
}

function Detail({ task, canCoordinate, isOwner, onClose, onAction, onCancel, onEdit }) {
  const { t } = useQueuePreferences();
  const [link, setLink] = useState(task?.finalPermalink || ''); const [reason, setReason] = useState(''); const [busy, setBusy] = useState(false); const [editing, setEditing] = useState(false); const [form, setForm] = useState({});
  useEffect(() => { setLink(task?.finalPermalink || ''); setReason(''); setEditing(false); setForm({ productionPoints: task?.productionPoints || 1, deadlineAt: localInputDate(task?.deadlineAt), tags: task?.tags?.join(', ') || '', brief: task?.brief || '', notes: task?.notes || '', references: task?.references?.join('\n') || '' }); }, [task?.id]);
  if (!task) return null;
  const run = async (action, value) => { setBusy(true); try { await onAction(action, value); } finally { setBusy(false); } };
  const save = async () => { setBusy(true); try { const saved = await onEdit({ ...form, productionPoints: Number(form.productionPoints), deadlineAt: new Date(form.deadlineAt).toISOString(), references: form.references.split('\n').map((item) => item.trim()).filter(Boolean) }); if (saved) setEditing(false); } finally { setBusy(false); } };
  const metric = (label, value) => <div className="metric" key={label}><span>{label}</span><strong>{value || '—'}</strong></div>;
  return <><button className="sidebar-backdrop" type="button" onClick={onClose} aria-label="Close" /><aside className="right-rail is-open" aria-label="Queue request details"><button className="rail-close-button" type="button" onClick={onClose} aria-label="Close"><X size={16} /></button><section className="panel detail"><div className="selected-post">{task.post.permalink ? <a className="selected-post-link" href={task.post.permalink} target="_blank" rel="noreferrer" title={t('sourcePost')}><div className="selected-post-media">{cover(task) ? <img className="cover-image" src={cover(task)} alt={`@${task.post.account}`} /> : <span className="cover-fallback"><strong>@{task.post.account}</strong></span>}<span className="media-badge">{statusCopy(task.status, t)}</span></div></a> : <div className="selected-post-media">{cover(task) ? <img className="cover-image" src={cover(task)} alt={`@${task.post.account}`} /> : <span className="cover-fallback"><strong>@{task.post.account}</strong></span>}</div>}</div></section><section className="panel caption-panel queue-rail-caption"><header className="panel-header caption-header"><div><p className="section-label">{editing ? t('editRequest') : statusCopy(task.status, t)}</p><h2>@{task.post.account}</h2></div>{canCoordinate && !editing ? <button type="button" className="ghost-button" onClick={() => setEditing(true)} title={t('editRequest')}><Pencil size={15} />{t('editRequest')}</button> : null}</header>{editing ? <div className="queue-detail-editor"><label>{t('productionPoints')}<input type="number" min="1" value={form.productionPoints || ''} onChange={(event) => setForm((current) => ({ ...current, productionPoints: event.target.value }))} /></label><label>{t('deadline')}<input type="datetime-local" value={form.deadlineAt || ''} onChange={(event) => setForm((current) => ({ ...current, deadlineAt: event.target.value }))} /></label><label>{t('tags')}<input value={form.tags || ''} onChange={(event) => setForm((current) => ({ ...current, tags: event.target.value }))} placeholder="design, copy" /></label><label>{t('brief')}<textarea value={form.brief || ''} onChange={(event) => setForm((current) => ({ ...current, brief: event.target.value }))} /></label><label>{t('notes')}<textarea value={form.notes || ''} onChange={(event) => setForm((current) => ({ ...current, notes: event.target.value }))} /></label><label>{t('referenceLinks')}<textarea value={form.references || ''} onChange={(event) => setForm((current) => ({ ...current, references: event.target.value }))} placeholder={t('oneLinkPerLine')} /></label></div> : <div className="queue-detail-copy"><p>{task.brief || task.post.caption || '—'}</p>{task.notes ? <section><h3>{t('notes')}</h3><p>{task.notes}</p></section> : null}{task.references?.length ? <section><h3>{t('references')}</h3>{task.references.map((item) => <a key={item} href={item} target="_blank" rel="noreferrer">{item}</a>)}</section> : null}</div>}<footer className="queue-detail-actions">{editing ? <><button className="scheduler-primary" disabled={busy || !form.productionPoints || !form.deadlineAt} onClick={save}>{t('saveChanges')}</button><button className="scheduler-secondary" disabled={busy} onClick={() => setEditing(false)}>{t('cancel')}</button></> : <>{task.status === 'scheduled' && isOwner ? <button className="scheduler-primary" disabled={busy} onClick={() => run('start')}>{t('startWork')}</button> : null}{task.status === 'in_progress' && isOwner ? <button className="scheduler-primary" disabled={busy} onClick={() => run('complete')}>{t('markComplete')}</button> : null}{task.status === 'completed' && isOwner ? <div className="scheduler-close-form"><label>{t('publishedLink')}<input value={link} onChange={(event) => setLink(event.target.value)} placeholder="https://instagram.com/p/..." /></label><button className="scheduler-primary" disabled={busy || !link.trim()} onClick={() => run('close', link)}>{t('closeRequest')}</button><button className="scheduler-secondary" disabled={busy} onClick={() => run('start')}>{t('returnInProgress')}</button></div> : null}{task.status === 'closed' && task.finalPermalink ? <a className="scheduler-primary" href={task.finalPermalink} target="_blank" rel="noreferrer">{t('openPublished')}</a> : null}{canCoordinate && !['closed', 'cancelled'].includes(task.status) ? <div className="scheduler-cancel"><input value={reason} onChange={(event) => setReason(event.target.value)} placeholder={t('cancellationReason')} /><button className="scheduler-danger" disabled={busy} onClick={() => onCancel(reason)}>{t('cancelRequest')}</button></div> : null}</>}</footer></section><section className="panel stats-panel">{metric(t('assignment'), task.designerEmail?.split('@')[0] || statusCopy(task.status, t))}{metric(t('deadline'), new Date(task.deadlineAt).toLocaleString())}{metric(t('scope'), `${task.productionPoints} PP · ${task.durationMinutes} ${t('minutes')}`)}{metric(t('recommendedAccount'), task.recommendedAccounts?.map((account) => `@${account}`).join(' · '))}</section></aside></>;
}

function Scheduler({ data, draft, setDraft, selectedDate, onOpen, onDragStart }) {
  const { t } = useQueuePreferences();
  const coordinator = data.viewer.isAdmin || data.viewer.operatingRoles?.includes('vc');
  const tasks = data.requests.filter((task) => task.scheduledDate === selectedDate && task.status !== 'pool');
  const [now, setNow] = useState(() => new Date());
  const [dropPreview, setDropPreview] = useState(null);
  useEffect(() => { const timer = window.setInterval(() => setNow(new Date()), 30000); return () => window.clearInterval(timer); }, []);
  const today = selectedDate === DAY(now);
  const viewStart = Math.max(0, currentMinutes(now) - 180);
  const earliestStart = today ? Math.ceil(currentMinutes(now) / 10) * 10 : 0;
  const dropCandidate = (event, designer) => { const id = Number(event.dataTransfer.getData('queue-task')); const source = data.requests.find((task) => task.id === id) || draft.find((task) => task.id === id); if (!source || source.status === 'in_progress') return null; const rect = event.currentTarget.getBoundingClientRect(); const pointerStart = Math.round(((((event.clientX - rect.left) / rect.width) * 720) + viewStart) / 10) * 10; const start = Math.max(START, today ? earliestStart : START, pointerStart); if (start + source.durationMinutes > 1440) return null; return { id, designer, start, duration: source.durationMinutes, account: source.post.account }; };
  const previewDrop = (event, designer) => { event.preventDefault(); if (!coordinator) return; const next = dropCandidate(event, designer); setDropPreview(next); };
  const drop = (event, designer) => { event.preventDefault(); if (!coordinator) return; const next = dropCandidate(event, designer); setDropPreview(null); if (!next) return; const source = data.requests.find((task) => task.id === next.id) || draft.find((task) => task.id === next.id); if (!source) return; setDraft((current) => [...current.filter((item) => item.id !== next.id), { ...source, designerEmail: designer, scheduledDate: selectedDate, scheduledStartMinutes: next.start, status: 'scheduled' }]); };
  const merged = (designer) => [...tasks.filter((task) => task.designerEmail === designer && !draft.some((entry) => entry.id === task.id)), ...draft.filter((task) => task.designerEmail === designer && task.scheduledDate === selectedDate)];
  const nowPosition = ((currentMinutes(now) - viewStart) / 720) * 100;
  return <section className="scheduler"><div className="scheduler-time-head"><span>{t('designer')}</span><div>{Array.from({ length: 12 }, (_, hour) => <b key={hour} style={{ left: `${hour * (100 / 12)}%` }}>{time(viewStart + hour * 60)}</b>)}</div></div>{data.designers.map((designer) => <div className="scheduler-row" key={designer.email}><header><b>{designer.email.split('@')[0]}</b><small>{designer.accounts?.map((account) => `@${account}`).join(' · ') || 'No accounts yet'}</small></header><div className="scheduler-track" onDragOver={(event) => previewDrop(event, designer.email)} onDragLeave={(event) => { if (!event.currentTarget.contains(event.relatedTarget)) setDropPreview(null); }} onDrop={(event) => drop(event, designer.email)}>{Array.from({ length: 13 }, (_, hour) => <i key={hour} style={{ left: `${hour * (100 / 12)}%` }} />)}{today ? <span className="scheduler-now" style={{ left: `${nowPosition}%` }}><b>{t('now')}</b></span> : null}{dropPreview?.designer === designer.email ? <span className="scheduler-drop-preview" style={{ left: `${((dropPreview.start - viewStart) / 720) * 100}%`, width: `${(dropPreview.duration / 720) * 100}%` }}><b>@{dropPreview.account}</b><small>{time(dropPreview.start)} · {dropPreview.duration} min</small></span> : null}{merged(designer.email).map((task) => <TaskBlock key={task.id} task={task} editable={coordinator} onOpen={onOpen} onDragStart={onDragStart} viewStart={viewStart} />)}</div></div>)}</section>;
}

function QueueApp({ user }) {
  const { t } = useQueuePreferences();
  const [data, setData] = useState(null); const [viewer, setViewer] = useState(null); const [date, setDate] = useState(DAY()); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); const [open, setOpen] = useState(null); const [draft, setDraft] = useState([]); const [accountChoice, setAccountChoice] = useState({}); const [adminOpen, setAdminOpen] = useState(false); const [report, setReport] = useState(null); const [reportLoading, setReportLoading] = useState(false); const [reportError, setReportError] = useState('');
  const load = useCallback(async () => { setLoading(true); try { const next = await json(`/api/dashboard/queue/v2?date=${date}`); setData(next); setDraft([]); setError(''); } catch (err) { setError(err.message || 'Queue could not load.'); } finally { setLoading(false); } }, [date]);
  useEffect(() => { load(); }, [load]);
  useEffect(() => { json('/api/dashboard/me').then(setViewer).catch(() => {}); }, []);
  const coordinator = data?.viewer && (data.viewer.isAdmin || data.viewer.operatingRoles?.includes('vc')); const pool = useMemo(() => data?.requests.filter((task) => task.status === 'pool') || [], [data]); const assigned = data?.assignedRequests || [];
  const toggleAdminTools = async () => { const next = !adminOpen; setAdminOpen(next); if (!next || report || reportLoading) return; setReportLoading(true); setReportError(''); try { setReport(await json('/api/dashboard/queue/v2/admin-report')); } catch (err) { setReportError(err.message || 'Could not load the admin report.'); } finally { setReportLoading(false); } };
  const submit = async () => { try { const changes = draft.map((task) => ({ id: task.id, designerEmail: task.designerEmail, scheduledDate: task.scheduledDate, scheduledStartMinutes: task.scheduledStartMinutes, recommendedAccounts: accountChoice[task.id] || task.recommendedAccounts || [] })); await json('/api/dashboard/queue/v2/submit', { method: 'POST', body: new URLSearchParams({ changes: JSON.stringify(changes) }) }); await load(); } catch (err) { setError(err.message); } };
  const action = async (actionName, value) => { try { const body = value ? new URLSearchParams(actionName === 'close' ? { final_permalink: value } : {}) : undefined; await json(`/api/dashboard/queue/v2/requests/${open.id}/${actionName}`, { method: 'POST', body }); setOpen(null); await load(); } catch (err) { setError(err.message); } };
  const cancel = async (reason) => { try { await json(`/api/dashboard/queue/v2/requests/${open.id}/cancel`, { method: 'POST', body: new URLSearchParams({ reason }) }); setOpen(null); await load(); } catch (err) { setError(err.message); } };
  const edit = async (values) => { try { const result = await json(`/api/dashboard/queue/v2/requests/${open.id}/edit`, { method: 'POST', body: new URLSearchParams({ production_points: String(values.productionPoints), deadline_at: values.deadlineAt, tags: values.tags, brief: values.brief, notes: values.notes, references: JSON.stringify(values.references) }) }); setOpen(result.request); await load(); return true; } catch (err) { setError(err.message); return false; } };
  return <main className="queue-page scheduler-page">
    <header className="queue-topbar"><div className="queue-brand"><CalendarDays size={22} /><div><span>sentientdash.app</span><h1>{t('productionQueue')}</h1></div></div><div className="queue-actions"><QueuePreferences />{data?.viewer?.isAdmin ? <button type="button" className={`queue-admin-button${adminOpen ? ' is-active' : ''}`} onClick={toggleAdminTools}><BarChart3 size={14} />{t('admin')}</button> : null}<a href={`${import.meta.env.BASE_URL}tracker.html`}>Tracker</a><a href={`${import.meta.env.BASE_URL}insights.html`}>Insights</a><a href={import.meta.env.BASE_URL}><ArrowLeft size={14} />{t('dashboard')}</a><button type="button" className="queue-avatar" title={user.email} onClick={() => { clearSsoCookie(); signOut(auth); }}><LogOut size={14} /></button></div></header>
    {loading ? <section className="queue-state"><LoaderCircle className="queue-spin" /><p>{t('loadingSchedule')}</p></section> : null}
    {error ? <section className="queue-state queue-error"><p>{error}</p><button type="button" onClick={load}>{t('tryAgain')}</button></section> : null}
    {data ? <>{adminOpen ? <AdminTools report={report} loading={reportLoading} error={reportError} onClose={() => setAdminOpen(false)} /> : null}
      <section className="scheduler-toolbar"><div><p className="scheduler-eyebrow">{coordinator ? t('coordinatorSchedule') : t('mySchedule')}</p><h2>{displayDate(date)}</h2></div><div className="scheduler-nav"><button type="button" aria-label="Previous day" onClick={() => setDate(shiftDay(date, -1))}><ChevronLeft size={17} /></button><button type="button" onClick={() => setDate(DAY())}>{t('today')}</button><button type="button" aria-label="Next day" onClick={() => setDate(shiftDay(date, 1))}><ChevronRight size={17} /></button></div>{coordinator && draft.length ? <button type="button" className="scheduler-submit" onClick={submit}><Send size={14} />{t('submit')} {draft.length} {draft.length > 1 ? t('changes') : t('change')}</button> : null}</section>
      {coordinator ? <section className="scheduler-pool"><header><div><p className="scheduler-eyebrow">{t('productionPool')}</p><h2>{pool.length} {t('readyToSchedule')}</h2></div><small>{t('visibleSchedule')}</small></header><div className="scheduler-pool-list">{pool.map((task) => <PoolCard key={task.id} task={task} onOpen={setOpen} onDragStart={() => {}} />)}{!pool.length ? <p className="scheduler-empty">{t('emptyPool')}</p> : null}</div></section> : null}
      {coordinator && draft.length ? <section className="scheduler-drafts">{draft.map((task) => { const designer = data.designers.find((item) => item.email === task.designerEmail); return <label key={task.id}>@{task.post.account} → {designer?.email.split('@')[0]}<select value={(accountChoice[task.id] || task.recommendedAccounts || [])[0] || ''} onChange={(event) => setAccountChoice((current) => ({ ...current, [task.id]: event.target.value ? [event.target.value] : [] }))}><option value="">No recommended account</option>{designer?.accounts?.map((account) => <option key={account} value={account}>@{account}</option>)}</select></label>; })}</section> : null}
      <Scheduler data={data} draft={draft} setDraft={setDraft} selectedDate={date} onOpen={setOpen} onDragStart={() => {}} />
      {!coordinator ? <DesignerAssignments tasks={assigned} onOpen={setOpen} /> : null}
    </> : null}
    <Detail task={open} canCoordinate={coordinator} isOwner={open?.designerEmail === data?.viewer.email || data?.viewer.isAdmin} onClose={() => setOpen(null)} onAction={action} onCancel={cancel} onEdit={edit} />
    <DevRolePreview isDev={Boolean(viewer?.is_dev || data?.viewer?.isDev)} />
  </main>;
}

function Root() {
  const [user, setUser] = useState(undefined); const [notice, setNotice] = useState(''); const [checked, setChecked] = useState(false);
  const [language, setLanguageState] = useState(() => window.localStorage.getItem('sentient.language') || (navigator.language.startsWith('es') ? 'es' : 'en'));
  const [theme, setThemeState] = useState(() => document.documentElement.getAttribute('data-theme') || 'dark');
  const [accent, setAccentState] = useState(() => window.localStorage.getItem('sentient.accent') || 'lime');
  const setLanguage = (value) => { window.localStorage.setItem('sentient.language', value); setLanguageState(value); };
  const setTheme = (value) => { window.localStorage.setItem('sentient.theme', value); document.documentElement.setAttribute('data-theme', value); setThemeState(value); };
  const setAccent = (value) => { window.localStorage.setItem('sentient.accent', value); document.documentElement.setAttribute('data-accent', value); setAccentState(value); };
  useEffect(() => { document.documentElement.setAttribute('data-accent', accent); }, [accent]);
  useEffect(() => { getRedirectResult(auth, browserPopupRedirectResolver).catch((error) => setNotice(describeSignInError(error))); }, []); useEffect(() => { trySsoSignIn().finally(() => setChecked(true)); }, []); useEffect(() => onAuthStateChanged(auth, setUser), []); useEffect(() => user ? startSsoRefresh() : undefined, [user]);
  const value = useMemo(() => ({ language, theme, accent, setLanguage, setTheme, setAccent, t: (key) => COPY[language]?.[key] || COPY.en[key] || key }), [language, theme, accent]);
  return <QueuePreferencesContext.Provider value={value}>{user === undefined || (!user && !checked) ? <main className="queue-auth" /> : user ? <QueueApp user={user} /> : <AuthGate notice={notice} setNotice={setNotice} />}</QueuePreferencesContext.Provider>;
}
ReactDOM.createRoot(document.getElementById('root')).render(<React.StrictMode><Root /></React.StrictMode>);
